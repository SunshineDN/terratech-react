import React from "react";
import {InputContainer, InputText} from "./styles";

export const Input = () => {
    return (
        <InputContainer>
            <InputText />
        </InputContainer>
    )
}